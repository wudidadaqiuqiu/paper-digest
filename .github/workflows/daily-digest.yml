name: Daily Paper Digest

on:
  schedule:
    # 每天北京时间 8:00 (UTC 0:00)
    - cron: '0 0 * * *'
  workflow_dispatch:  # 支持手动触发

jobs:
  digest:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r paper-digest/requirements.txt

      - name: Run paper digest
        env:
          QQ_PASSWORD: ${{ secrets.QQ_PASSWORD }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
        run: python paper-digest/main.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./paper-digest/output
          keep_files: true  # 保留历史 digest 文件
